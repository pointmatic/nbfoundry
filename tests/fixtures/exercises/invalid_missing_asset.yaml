# Copyright (c) 2026 Pointmatic
# SPDX-License-Identifier: Apache-2.0
# Rejection: image expected_output references an asset that does not exist under base_dir.
title: Missing asset exercise
description: References a nonexistent image.
sections:
  - title: Plot it
    description: Produce the plot.
    code: |
      plot(df)
expected_outputs:
  - description: A scatter plot.
    type: image
    path: assets/does_not_exist.png
    alt: A plot that is not on disk
