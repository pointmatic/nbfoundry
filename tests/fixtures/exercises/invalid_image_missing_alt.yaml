# Copyright (c) 2026 Pointmatic
# SPDX-License-Identifier: Apache-2.0
# Rejection: image expected_output requires both `path` and `alt`; `alt` is missing.
title: Image missing alt exercise
description: Image expected output without alt text.
sections:
  - title: Plot it
    description: Produce the plot.
    code: |
      plot(df)
expected_outputs:
  - description: A scatter plot.
    type: image
    path: assets/plot.png
