# Copyright (c) 2026 Pointmatic
# SPDX-License-Identifier: Apache-2.0
# Rejection: a `range` rule requires field type `number`; here it is on a `text` field.
title: Range on text field exercise
description: Mismatched rule/field type.
sections:
  - title: Step one
    description: Do the thing.
    code: |
      print("hello")
submission:
  pass_threshold: 0.5
  fields:
    - name: answer
      type: text
      label: Answer
      expected:
        type: range
        min: 0.0
        max: 1.0
